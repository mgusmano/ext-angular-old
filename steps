delete dist/ folder
delete /src/
create src/app folders
go to ext-angular folder
node all.js (fills src/app folders)
tsc (creates dist/app folder)
cp -r demo dist/demo/
(commit to git)
npm version patch
cp README.md dist/
cp package.json dist/
cd dist
npm publish
